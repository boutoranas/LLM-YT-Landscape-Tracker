# Copy this to .env and fill values

# Used for Vertex AI auth
PROJECT_ID=
VERTEX_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=service_account.json

# Optional overrides
GEMINI_MODEL=gemini-2.5-flash
